{
  "name": "saas-plat-erp-base-department",
  "version": "1.0.0",
  "description": "部门管理",
  "scripts": {
    "compile": "babel --presets es2015,stage-1 --plugins transform-runtime src --out-dir app --source-maps",
    "watch": "babel --watch --presets es2015,stage-1 --plugins transform-runtime src --out-dir app --source-maps",
    "pub": "npm publish --registry https://registry.npmjs.org/",
    "prepublish": "npm run compile"
  },
  "keywords": [
    "erp",
    "base",
    "department"
  ],
  "author": "saas-plat.com",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-1": "^6.24.1"
  }
}
